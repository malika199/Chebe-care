# Production : racine de l’API (HTTPS, sans / final, sans /api — voir client/.env.example).
VITE_API_URL=https://votre-api.onrender.com

# Optionnel : URL du site client (aperçu des chemins non /images dans l’admin).
# VITE_CLIENT_URL=https://www.tondomaine.com
