# Chargé automatiquement par Vite lors de `npm run build` (production).
# Les images utilisent cette URL pour préfixer /images/... — obligatoire si le site n’est pas sur le même domaine que l’API.
VITE_API_URL=https://chebe-care.onrender.com
